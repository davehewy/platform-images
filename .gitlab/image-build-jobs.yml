variables:
  PLATFORM_IMAGES_RUNNER_TAG: "podman"

.image-build:
  stage: build
  interruptible: true
  tags:
    - $PLATFORM_IMAGES_RUNNER_TAG
  before_script:
    - platform images validate
    - platform images registry-login
